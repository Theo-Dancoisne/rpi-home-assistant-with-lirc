#!/sbin/openrc-run
# Custom OpenRC service for LIRC


description="LIRC Daemon for Infrared Remote Control"
command="/usr/sbin/lircd"
# command_args="--nodaemon --driver=default --device=/dev/lirc0"
pidfile="/var/run/lirc/lircd.pid"

depend() {
    need localmount
    after bootmisc
}
